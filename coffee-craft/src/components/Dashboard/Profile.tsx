import { UserPageProps } from '@/types/types'
import React from 'react'

export default function Profile(  { page,
    title,
    description,
    userId,
    formId,
  }: UserPageProps) {
  return (
    <div>Profile</div>
  )
}
